import React from 'react';
import {StatusBar as SB} from 'react-native';

const StatusBar = ()=>{

    return <SB  translucent={false} backgroundColor='white' barStyle='dark-content'/>
};

export default StatusBar;